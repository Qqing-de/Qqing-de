<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="qq.dao.UserMapper">
    <sql id="allFields">id, userName, password, sex, age, telephone, Email, address, pic, state</sql>

    <update id="retrieve" parameterType="qq.pojo.User">
        UPDATE user SET password = #{password} WHERE userName= #{userName}
    </update>

    <select id="findLogin2" parameterType="qq.pojo.User" resultType="int">
        select count(*) from user where userName=#{userName}
    </select>
</mapper>